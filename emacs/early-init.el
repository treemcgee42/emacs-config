(setq site-run-file nil)
(setq inhibit-default-init t)
(setq package-enable-at-startup nil)

;; Allow upgrading built-in packages (required for transient, seq, etc.)
(setq package-install-upgrade-built-in t)
